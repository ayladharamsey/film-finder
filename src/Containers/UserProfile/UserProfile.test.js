import React from 'react';
import { shallow } from 'enzyme';
import UserProfile from './UserProfile';

describe('UserProfile container tests', () => {

  it('Should make sure the test is firing', () => {
    expect(true).toEqual(true);
  })
})